name: yahoo_finance
description: Unofficial library for using yahoo finance api
version: 0.0.1
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio:
  pretty_dio_logger:
  enum_to_string:

dev_dependencies:
  flutter_test:
    sdk: flutter
